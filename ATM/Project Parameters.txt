The goal of this project is to create a class that acts as a virtual ATM machine and lets the user check their balance, deposit, withdraw, create accounts, and close accounts. when running the project will initially take in the attached customers file and create a database of users for the ATM machine. the user can then access these accounts if they have the correct account number, or can create a new account. 